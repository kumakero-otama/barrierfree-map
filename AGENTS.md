# StepBy / barrierfree-map 絶対ルール

このファイルのルールは、StepBy・barrierfree-mapに関わるすべての調査、実装、テスト、運用作業より優先する。

## OSMへの書込み

- ユーザー（プロジェクト管理者）が、その時点の依頼で本番OSMへの実送信を明示的に許可するまで、OSMへのマージ、changeset作成・アップロード、Node・Way・Relationの作成・変更・削除を絶対に実行しない。
- 過去の許可、一般的な「開発を進めて」という依頼、計画への同意、テストの許可を、本番OSMへの送信許可として扱わない。許可は対象と操作が確認できる一回の作業に限定し、次回へ持ち越さない。
- 許可がない状態では、OSM連携をdry-run、変更案の生成、確認画面、ローカルテスト、OSM開発環境でのテストに限定する。
- OSM書込み機能を実装する場合も、初期状態を無効にし、サーバー側の明示的な機能フラグと操作直前の管理者確認が揃わなければ送信できない構造にする。

## OSMマージ履歴

- OSMへの送信要求、変更案、確認者、実行者、日時、対象、変更前後、changeset ID、Way・Node・RelationのIDとVersion、成功、失敗、競合、エラー、取消要求、取消結果を、例外なく追記型の監査履歴へ保存する。
- 監査履歴は削除・上書きしない。訂正や状態変更も新しい履歴イベントとして追加する。
- 履歴の保存を開始前に保証できない場合、または保存に失敗した場合は、OSMへの送信を実行しない。OSM送信より履歴の確実な保存を優先する。
- OSMアクセストークン、秘密鍵、Cookieなどの秘密情報は監査履歴へ保存しない。

## OSMマージの取消し

- 取消しは元の履歴を削除する処理ではない。現在のOSMデータとVersionを再取得し、安全に戻せる反対変更を新しいchangesetとして作成する。
- 取消しも本番OSMへの新しい書込みとして扱い、操作ごとにユーザーの明示許可を必要とする。
- 競合や第三者による後続編集がある場合は自動で上書きせず、取消可能部分と不可能部分を提示して停止する。
- 取消要求、確認、実行内容、部分取消、失敗を含む全結果を追記型監査履歴へ保存する。

## 本番と開発の分離

- 本番のmain、プロセス、ポート、DB、公開UIを、開発作業のために切り替えたり停止したりしない。
- 開発はdevブランチ、別worktree、別プロセス、別ポート、開発専用設定で行う。
- 開発環境を本番DBへ接続しない。本番への反映は、確認済み変更を明示的に本番へ昇格するときだけ行う。
